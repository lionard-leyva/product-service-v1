spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/productdb
    username: root
    password: root

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/productdb
    user: root
    password: root
    locations: classpath:dev/migration

logging:
  level:
    org.springframework.r2dbc.core: DEBUG
