spring:
  r2dbc:
  # La URL, username y password serán proporcionados dinámicamente por TestDatabaseConfiguration
  flyway:
    enabled: true
    schemas: productdb
    locations: classpath:db/migration
    default-schema: productdb

logging:
  level:
    org.springframework.r2dbc: DEBUG
    org.flywaydb: DEBUG